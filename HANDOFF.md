# 引き継ぎメモ（クチコミ返信AI）

Leaves Bali（バリ島拠点・鈴）が開発しているクチコミ返信AIの現状です。

Googleに届くクチコミを自動で取得し、AIが返信案を3つ（丁寧・標準・簡潔）作ります。
バリ島のホテル **Hoshi Jungle** 向けに作ったものを、**日本（福岡）の飲食店・ホテル**
にも売れるように直しているところです。

## リポジトリ

- GitHub: `leaves-bali/leaves-bali-lp` — **公開リポジトリです**（誰でも中身を読めます）
- 作業ブランチ: `claude/fervent-noether-ddz5a7`
- `main` は静的LPのみで、Vercel（`leaves-bali-lp.vercel.app`）に公開中

```
hoshi-jungle-review-ai/     Next.jsアプリ本体（未デプロイ）
hoshi-jungle/index.html     Hoshi Jungle向け提案ページ
hoshi-jungle/demo/          体験デモ（単一HTML・JS必須）
sales/review-ai/slides.html 営業資料のソース（これを編集してPDFを作る）
sales/review-ai/build.mjs   PDF生成スクリプト（はみ出し・重なりを検査する）
```

---

# Suzuがやること

ここに書いてあることを**Suzuがやらないと、機能は動きません**。
専門用語は使わずに書いています。上から順にやれば終わります。

## 0. Google の利用申請を出す（これが無いと何も動きません）

**Googleのクチコミを取り込むには、Googleに利用申請を出して通る必要があります。**
Google Cloud でプロジェクトを作ってAPIを有効にしただけでは、
使える回数がゼロのままで、1件も取得できません。

承認までは**数営業日〜数週間**かかります。**開発の完成を待たずに、先に出してください。**
このプロジェクトでいちばん時間がかかるのはここです。

手順は `hoshi-jungle-review-ai/docs/02-google-oauth-flow.md` の §3 にあります。

申請が通るまでの間も、次の2つはできます。
- 体験デモ（`hoshi-jungle/demo/`）でお店に見せる
- Google以外のクチコミを貼り付けて、AIの返信の質を確かめる（オプション契約が要ります）

## 1. 店舗ごとの設定を入れる

お店の名前・場所・返信の署名・連絡先・お店の魅力を、**お店ごとに**登録できるように
なりました。登録しないと、これまで通りバリ島のホテル（Hoshi Jungle）の情報が
使われます。

**やり方**：オーナーとしてログイン →「パスコード管理」の画面 → 一番上の「お店の設定」

**いちばん大事なのは「お店の魅力」の欄です。**
AIは、ここに書かれていることにしか触れません。空っぽのままだと、
クチコミに書いてあること以外は何も書けない返信になります。
逆に、ここに書いていない設備やサービスをAIが勝手に書くこともありません。

例（寿司屋なら）:
```
カウンター8席の小さな店
朝に市場で仕入れた魚だけを使う
アラカルトはなく、おまかせのみ
```

## 2. 契約する機能を切り替える（お店ごと）

2つのオプションは**既定で「契約なし」**です。契約なしのお店には、
画面にもメニューにも出てきません。

| 機能 | 何ができるか |
|---|---|
| 月次の改善レポート | 毎月1日に、先月のまとめが自動で作られる |
| Google以外のサイト | 食べログなどのクチコミを貼り付けて返信案を作れる |

**やり方**：Supabase の画面 → SQL Editor → 下をコピーして実行

```sql
-- いまの契約状況を見る
select location_id, name, report_enabled, other_sites_enabled from locations;

-- 契約を有効にする（location_id は上で出てきた値に置き換える）
update locations
   set report_enabled = true, other_sites_enabled = true
 where location_id = 'ここに貼る';
```

解約するときは `true` を `false` に変えるだけです。データは消えません。

## 3. GitHub の設定を1つ足す（月次レポート用）

月次レポートは毎月1日の朝10時（日本時間）に自動で作られます。
そのために GitHub 側の設定が1つ要ります。

**やり方**：GitHub のリポジトリ → Settings → Secrets and variables → Actions
→ **Variables** タブ → `NEXT_PUBLIC_APP_URL` を追加（値はアプリを公開したURL）

他の値（Supabaseの鍵、Anthropicの鍵など）は同期の設定ですでに登録済みなので、
追加で入れるものはありません。

## 4. メール送信の設定をする（Resend）

月次レポートができたら、お店のメールアドレスに自動で送ります。
**Resend で実装済み**ですが、下の設定をしないとメールは送られません
（レポート自体は作られ、画面では見られます）。

無料枠は月3,000通・1日100通。月1回・お店の数ぶんなので、当分無料で収まります。

**やり方**

1. https://resend.com に登録する（無料）
2. **Domains** で Leaves Bali のドメインを追加する
   → 画面に DNS レコードが3つほど出るので、ドメインを管理している画面で登録する
   → **これをしないと送れません**（他人のドメインを騙れないようにするための仕組み）
3. **API Keys** で鍵を1つ作る（`re_` で始まる文字列）
4. GitHub → リポジトリ → Settings → Secrets and variables → Actions
   - **Secrets** タブ → `RESEND_API_KEY` を追加（3で作った鍵）
   - **Variables** タブ → `REPORT_EMAIL_FROM` を追加
     （例: `Leaves Bali <report@leavesbali.com>` — 2で認証したドメインのアドレス）

**送り先**は、お店の設定に入れた「低評価のときの連絡先」のアドレスです。
入れていないお店には送られません。

鍵を入れなくてもシステムは止まりません。メールが送られないだけです。
**送っていないのに「送信済み」になることはありません。**

## 5. アプリをまだ公開していません

アプリ本体はデプロイしていないため、**実際に動くURLがありません**。
お店に見せられるのは、いまのところ体験デモだけです。
公開先（Netlify か Vercel）を決めたら、手順は `docs/04-runbook.md` にあります。

## 6. 体験デモの色が古いままです

体験デモは GitHub Pages で公開しています。
**Pages は `main` ではなく `claude/customer-icon-details-51lkfm` ブランチから配信されています。**
今回ソフトウェア本体を赤白に変えたので、デモも赤白に直しましたが、
そのブランチにはまだ反映していません（勝手に他のブランチへ入れないためです）。
**「デモも赤白にして」と言っていただければ反映します。**

---

# いまできること・できないこと

## できるようになったこと（今回）

1. **お店ごとの設定** — 店名・所在地・署名・連絡先・お店の魅力・自動公開の条件を
   お店ごとに持てるようになりました。設定していないお店はこれまで通り動きます。
2. **お店ごとの契約** — 月次レポートとGoogle以外のサイトを、お店ごとに入り切りできます。
   契約していないお店には画面にも出ず、URLを直接叩いてもエラーになります。
3. **Google以外のクチコミ** — 食べログ・Agoda・トリップアドバイザー・Trip.com・
   Booking.com・Expedia のクチコミを**貼り付けて**返信案を作れます。
   Googleと同じ3案が出ます。
4. **月次の改善レポート** — 件数・平均評価・言語別の件数・返信できた割合（すべて前月比）と、
   褒められた点・指摘された点（件数つき）・来月やること3つ。毎月1日に自動で作られ、
   お店のメールアドレスに Resend で送られます（上記4の設定が要ります）。
5. **赤白の配色** — ソフトウェアも営業資料も白地に赤で統一しました。

## できないこと（正直に）

- **Google以外のサイトには、このシステムから返信を投稿できません。**
  各社が返信の仕組みを外部に公開していないためで、審査や交渉の問題ではありません。
  スタッフがコピーして、各サイトの管理画面に貼り戻す形になります。
  「貼り戻しました」の印は押せますが、**それは押した記録であって、
  実際に投稿された保証ではありません。**
- **食べログのクチコミを自動で取り込むことはできません。**
  利用規約第9条で情報の自動収集が禁止されています。人が貼る前提で作っています。
- **月次レポートのメールは、Resend の設定（上記4）を終えるまで送られません。**
  レポート自体は作られ、画面では見られます。

---

# 調査済みの事実（再調査は不要です）

## 対応できるプラットフォーム

**完全自動で返信できるのはGoogleだけ**です。これは審査の問題ではなく、
各社が返信用のAPIを外部に公開していないためで、技術的に回避できません。

| | 取得 | 自動返信 | 備考 |
|---|---|---|---|
| Google | ✅ | ✅ | 取得から公開まで全自動 |
| Booking.com | ✅ | ⚠️ | APIはあるが認定パートナー限定。**新規受付停止中** |
| Expedia / Hotels.com | ✅ | ⚠️ | `setReviewResponse` あり。審査必要 |
| Agoda | ❌ | ❌ | 返信APIなし |
| TripAdvisor | 読取のみ | ❌ | 投稿APIなし |
| Trip.com | ❌ | ❌ | 返信APIなし |
| 食べログ / Retty | ❌ | ❌ | APIなし。ぐるなびは読み取りのみ |

→ **「自動返信システム」という名前は使わないでください。** Google以外では事実と違います。
　 資料では「クチコミ返信AI」としています。

## 価格の相場

| 対象 | 相場 |
|---|---|
| 口コミ返信代行 | 月3〜5万円 |
| MEO対策（飲食店・月額固定） | 月3〜5万円 |
| AI返信ツール単体・月20件まで | 月3,000〜8,000円 |
| AI返信ツール単体・月100件まで | 月10,000〜20,000円 |
| スマートMEO | 初期0円・月38,500円 |
| EmbedSocial | 1店舗 月1,100円〜 |
| 口コミコム / Canly / MEO Dash | 価格非公開（要問い合わせ） |

**注意点**：3〜5万円はMEO対策一式（順位対策・投稿代行・分析・コンサル）込みの価格帯です。
返信特化で同額だと割高に見えます。

**コスト削減の訴求には限界があります**：返信は1件3〜5分、月30件で2.5時間。
店長の時給3,000円換算で月7,500円相当にしかなりません。
「コスト削減」で売るなら、**すでに代行業者に月3〜5万円払っている店**に
「それを下げます」と言うのが唯一筋が通ります。

## 日本の中小企業のAI導入（資料の根拠）

| 数字 | 出典 |
|---|---|
| 導入率 20.4%（検討中18.6%・計39.0%） | 中小企業基盤整備機構 2026年3月 |
| 大企業 42〜48%（約5倍の差） | Leach 中小企業AI導入実態調査2026 |
| 「何から始めればいいか分からない」62%（1位） | 同上 |
| セキュリティが不安 31% / 経営層の理解 28% | 同上 |
| 返信1件あたり3〜5分 | 飲食店向け運用記事 |
| 訪日客「言葉が通じない」32.9% | インバウンド調査 |

**重要**：「経営者の8〜9割がAIに抵抗感」という見立ては、データ上は
「8割が未導入」であって抵抗ではありません。1位の理由は「分からない」です。
だから説得より**体験デモを触らせる**構成にしています。

## 福岡の戦略

**福岡市東区は観光地ではなく、福岡市で人口最多のベッドタウン**（香椎・千早＝
子育て世代の住宅地）です。東区の寿司屋ではインバウンド客が少なく、
**多言語対応は武器になりにくい**可能性があります。

多言語が効くのは **中央区（天神・大名）・博多区**です。福岡は韓国からの玄関口で、
訪日客は韓国が全国1位（月89万人）。

---

# 作業のルール（次に触る人へ）

- **色は必ずコントラスト比を実測してから決めてください。** 以前、小さい文字が3.52:1で
  基準割れしていて「見にくい」と指摘を受けています。
  現在使っている値と実測値は `hoshi-jungle-review-ai/tailwind.config.ts` にあります。
- **PDFを作ったら必ず検査してください。** `sales/review-ai/build.mjs` が
  「はみ出し」と「ブロック同士の重なり」を検査します。以前、枠内には収まっているのに
  本文と注意書きが14px重なったまま出力しかけました。
- playwright はこのリポジトリの依存に入っていません。グローバル導入のものを使い、
  ブラウザは `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` を
  `executablePath` で直接指定します（`playwright install` は不要）。
- コミットメッセージは日本語で、**なぜそうしたか**を書いてください。
- **公開リポジトリです。秘密情報を絶対にコミットしないこと。**

# 補足

- Supabaseプロジェクト: `iwwddgqbollzzmzjrvak`（マイグレーション 0001〜0013 適用済み）
  - 無料プランは1週間ほど使わないと自動で止まります。`keep-supabase-awake.yml`
    （3日に1回アクセスする）を入れてあるので、通常は止まりません。
- 体験デモ（GitHub Pages）: `https://leaves-bali.github.io/leaves-bali-lp/hoshi-jungle/demo/`
  - **配信元は `claude/customer-icon-details-51lkfm` ブランチです**（`main` ではありません）
- アプリ本体はまだデプロイしていないため、実際に動くURLはありません
